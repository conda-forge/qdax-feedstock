{% set name = "qdax" %}
{% set version = "0.2.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/qdax-{{ version }}.tar.gz
  sha256: c35c6ac3d1c3dfa722f9f2b6a4583cd5d0c59a95f4c57e5b66c2eff2072bcdd0

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.8
    - pip
  run:
    - python >=3.8
    - numpy
    - absl-py >=1.0.0
    - jax >=0.3.10
    - jaxlib >=0.3.10
    - flax >=0.4.1
    - brax >=0.0.12
    - gym >=0.24.0
    - scikit-learn >=1.0.2
    - scipy >=1.8.0

test:
  imports:
    - qdax
  commands:
    - python -c "from qdax.core.map_elites import MAPElites; from qdax.utils.metrics import default_qd_metrics"

about:
  home: https://github.com/adaptive-intelligent-robotics/QDax
  summary: A Python Library for Quality-Diversity and NeuroEvolution
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - Tobias-Fischer
